# whisper-pro

Dify tool plugin for Whisper-compatible transcription with bundled FFmpeg (Linux amd64).

Marketplace package needs `README.md` (English, GitHub source URL), `PRIVACY.md`, and `manifest.yaml` fields `privacy`, `repo`, `contact`. `requirements.txt` must not use `--no-index`; pin `dify-plugin>=0.10.2,<0.11.0`. Drop one screenshot at `_assets/screenshots/overview.png`.

Last updated: 2026-09-10
